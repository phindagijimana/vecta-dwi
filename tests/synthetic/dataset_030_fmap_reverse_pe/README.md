# dataset_030_fmap_reverse_pe

Reverse-PE reference lives in fmap/ as an EPI (BIDS-standard for
prospective studies) with IntendedFor linking back to the DWI.

Expected: VECTA.DWI.ACQ.REVERSE_PE_AVAILABLE=true (via fmap path),
VECTA-DWI-014 satisfied, readiness=ready.
